import { ai } from "./ai";
import { app } from "./app";
import { project } from "./project";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";

export const router = {
  ai,
  theme,
  window,
  app,
  project,
  shell,
};
