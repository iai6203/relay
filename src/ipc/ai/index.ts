import {
  chat,
  deleteSession,
  getSession,
  getSessions,
  respondToPermission,
} from "./handlers";

export const ai = {
  chat,
  deleteSession,
  getSession,
  getSessions,
  respondToPermission,
};
